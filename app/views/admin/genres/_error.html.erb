<% if genre.errors.any? %>
  <div class="alert alert-danger">
      The form contains <%= pluralize(genre.errors.count, "error") %>.
      <ul>
      <% genre.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>