<div class="container">
  <div class="row">
    <div class="col-lg-9">
      <h1 class="item-header"><b>商品一覧</b></h1>

      <%= link_to new_admin_item_path do %>
        <i class="fas fa-plus-circle fa-3x new-botton"></i>
      <% end %>

      <%= render "table", items: @items %>
      <%= paginate @items %>
    </div>
  </div>
</div>
